<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Keep Wine startup deterministic in the browser: do not scan host paths. -->
  <dir>/opt/wine/share/wine/fonts</dir>
  <cachedir>/var/cache/fontconfig</cachedir>
</fontconfig>
